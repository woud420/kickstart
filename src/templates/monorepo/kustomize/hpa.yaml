apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ monorepo_name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ monorepo_name }}
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ '{{ include "example-service.fullname" . }}' }}-config
data:
{{ '{{- range $key, $value := .Values.env }}' }}
  {{ '{{ $key }}' }}: {{ '{{ $value | quote }}' }}
{{ '{{- end }}' }}
